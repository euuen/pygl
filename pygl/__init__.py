from pygl.world     import *
from pygl.camera    import *
from pygl.model     import *
from pygl.logs      import *

logs(INFO, "pygl 2024-1-1更新")
logs(INFO, "pygl的作者很懒，没有写事件处理的接口函数。但是您可以用pygame的函数来处理事件")
logs(INFO, "目前的图元类型只支持POINTS，还是作者懒")
logs(INFO, "用户可能会错误的使用函数，但是由于作者精力有限，只做了一点点错误提示")
logs(INFO, "目前还不能自由调节点的颜色")
logs(INFO, "目前还支持右手坐标系")
